  class Main {

    Main(){
      
      // graph.fillGraph(maxNodes);

      // // graph.addEdge(2.1, 1, 2);
      // // graph.addEdge(1.2, 2, 3);
      // // graph.addEdge(3.4, 3, 4); // I forgot this line in the video
      // // graph.addEdge(5.6, 4, 5);
      // // graph.addEdge(0.2, 5, 6);
      // // graph.addEdge(3.4, 6, 7);
      // // graph.addEdge(6, 7, 8);
      
      // graph.randomEdge2(5, maxNodes);
      // graph.print();
      // graph.depthFirstSearch(8);
    }
    public static void main(String[] args) {
      new GUI();    
    }
  }